import Layout from 'components/Layout'
import { P } from 'mui/Typography'

const Theme = () => {
  return (
    <Layout title="Material-UI Theme Integration">
      <P>To update the colors edit <code>theme.js</code> in the root of your project.</P>
    </Layout>
  )
}

export default Theme